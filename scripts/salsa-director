#!/usr/bin/env bash
# scripts/agency-director — Startet den Agency Director
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_ROOT="$(dirname "$SCRIPT_DIR")"

cd "$PROJECT_ROOT"
exec claude --append-system-prompt "$(cat "$PROJECT_ROOT/main-agent.md")"
